const ifj = @import("ifj24.zig");

pub fn main() void { // Nested while loops
    var i: i32 = 0;
    var j: i32 = 0;
    while (i < 5) {
        j = 0;
        while (j < 5) {
            ifj.write(i);
            ifj.write(j);
            j = j + 1;
        }
        i = i + 1;
    }
}